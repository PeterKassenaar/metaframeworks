---
import Layout from '../layouts/Layout.astro';
import FrameworkCard from '../components/FrameworkCard.astro';
import { frameworks } from '../data/frameworks';
---

<Layout title="Web Metaframeworks Directory">
	<main>
		<h1>Web <span class="text-gradient">Metaframeworks</span></h1>
		<p class="instructions">
			A comprehensive directory of modern web metaframeworks for building scalable applications.
			Listed are metaframeworks for
			<a href="https://react.dev/" target="_blank">React</a>,
			<a href="https://angular.dev" target="_blank">Angular</a>,
			<a href="https://vuejs.org/" target="_blank">Vue</a>
			and some framework-agnostic tools.
		</p>
		<ul role="list" class="link-card-grid">
			{frameworks.map((framework) => (
				<FrameworkCard
					href={framework.href}
					title={framework.title}
					body={framework.body}
					logo={framework.logo}
				/>
			))}
		</ul>
		<hr>
		<FrameworkCard
			href="https://www.kassenaar.com/contact"
			title="Want to book a training?"
			body="If you or your employees are in need of more React, Angular or Vue knowledge, or about one of the metaframeworks listed here, why don't you book a training? Contact us for options."
			logo="https://www.kassenaar.com/favicon.ico"
		/>

	</main>
</Layout>

<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: rgb(var(--accent-dark));
		font-size: 20px;
		line-height: 1.6;
	}
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 1em;
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0;
	}
	.instructions {
		margin-bottom: 2rem;
		border: 1px solid rgb(var(--accent-light));
		background: #f8fafc;
		padding: 1.5rem;
		border-radius: 8px;
		text-align: center;
		color: rgb(var(--accent-dark));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 2rem;
		padding: 0;
	}
</style>
